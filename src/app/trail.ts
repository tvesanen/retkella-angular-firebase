import { Position } from './position';

export class Trail {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
}

export class TrailImage {
  id: string;
    imageLat: number;
    imageLon: number;
    imageSrc: string;
}
